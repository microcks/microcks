# microcksId: Movie Graph Original API : 1.0
schema {
    query: Query
}
type Film {
    id: String!
    title: String!
    episodeID: Int!
    director: String!
    starCount: Int!
    rating: Float!
    comment: String!
}

type Query {
    film(id: String): Film
}

